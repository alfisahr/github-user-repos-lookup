const UserResult = ({userResult}) => {
	console.log(userResult)
	return (
		<div className="container">
			<div className="sidebar">ini sidebar</div>
			<div className="content">ini kontent</div>
		</div>
	);
};

export default UserResult;
